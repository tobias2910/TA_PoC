/**
 * This interface is used to store all relevant user data necessary
 * for authorization.
 */
export interface IUserData {
    firstName: string;
    lastName: string;
    faceId: string;
    userId: string;
    mail: string;
}